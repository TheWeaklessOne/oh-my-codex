interface:
  display_name: "Upstream Sync"
  short_description: "Sync fork main to the current upstream release"
  default_prompt: "Use $upstream-sync to merge the current upstream release into local main, optionally rebase one named work branch onto the refreshed main, and relink the repo CLI when the current checkout is main."
